name: Build and Deploy

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: self-hosted

    steps:
    - name : Checkout
      uses: actions/checkout@v6.1.0
    - name: List contents of the repository
      run: ls -A